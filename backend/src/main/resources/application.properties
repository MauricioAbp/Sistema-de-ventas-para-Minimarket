spring.application.name=gruposowad

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/minimarket}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:77016141}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=${SERVER_PORT:8080}

app.jwt.secret=${JWT_SECRET:12345678901234567890123456789012}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
